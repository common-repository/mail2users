$j = jQuery.noConflict();  //select users for send email $j('document').ready(function(){     //select users for send email groups     $j(".mail2users-group li").click(function(){         if(!$j(this).hasClass('selected')){             $j(this).addClass('selected');             var email = $j(this).data('email');             $j('.mail2users-to').append('<span>'+email+'</span>');             $j('.mail2users-to-ta').append(email+',').attr('name','mail2users_groups');             $j(".mail2users-users").hide('slow');         }         else{             alert('Already Selected');         }     });     //select users for send email individual user     $j(".mail2users-users li").click(function(){         if(!$j(this).hasClass('selected')){             $j(this).addClass('selected');             var email = $j(this).data('email');             $j('.mail2users-to').append('<span>'+email+'</span>');             $j('.mail2users-to-ta').append(email+',').attr('name','mail2users_indv');             $j(".mail2users-group").hide('slow');         }         else{             alert('Already Selected');         }     });// media uploader     if ($j('.set_custom_images').length > 0) {         if ( typeof wp !== 'undefined' && wp.media && wp.media.editor) {             $j('.mail2users-compose-section').on('click', '.set_custom_images', function(e) {                 e.preventDefault();                 var button = $j(this);                 var id = button.prev();                 wp.media.editor.send.attachment = function(props, attachment) {                     id.val(attachment.url);                 };                 wp.media.editor.open(button);                 return false;             });         }     }; });//check box at post page$j('document').ready(function(){    $j(".group_checkbox").change(function() {        if(this.checked) {            $j(this).val("true");        }        else{            $j(this).val("");        }    });});